<div class="container">
	<h2>当前团体有<%= Fangroup.count %>个，加入其中，或者创建自己的团体</h2><br>
	<table class="table">
		<% @fangroups.each do |fangroup| %>
			<% @fangroup = fangroup %>
			<%= render "showfangroup" %>
		<%end%>
	</table>

<hr>
	<h2>创建新团体</h2>
	<%= render "newfangroup" %>
</div>
