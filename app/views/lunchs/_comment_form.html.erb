<%= form_for(@comment) do |f| %>
	<%= f.hidden_field :lunch_id, value: @lunch.id %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.text_field :content %>
  <%= f.submit "评论", class: "btn btn-small" %>
<% end %>
